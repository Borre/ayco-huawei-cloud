# OCR Service activation
# NOTE: Huawei Cloud OCR is an API service, not a provisionable resource.
# Activate via console or API: POST /v2/{project_id}/ocr/service-activation
# For demo, the FunctionGraph ocr_trigger.py calls the OCR API directly.
